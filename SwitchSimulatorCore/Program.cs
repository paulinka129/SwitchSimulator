using System;
using System.Collections.Generic;

namespace SwitchSimulatorCore
{
    public class Program
    {
        static void Main(string[] args)
        {
            //Switch Switch01 = new Switch();
            ////Computer Computer01 = new Computer(DataGenerator.);
            //var Computers = new List<IDevice>();
            //for (int i = 1; i < 9; i++)
            //{
            //    Computer computer = new Computer(DataGenerator.GenerateMACAddress(i), DataGenerator.GenerateDeviceName(i, typeof(IDevice)));
            //    Computers.Add(computer);
            //}

            //Switch01.AddDevice(0,Computers[0]);
            //Switch01.AddDevice(1,Computers[1]);
            //Switch01.AddDevice(2, Computers[2]);
            //Switch01.AddDevice(3, Computers[3]);
            //Switch01.AddDevice(4, Computers[4]);
            //Switch01.AddDevice(5, Computers[5]);
            //Switch01.AddDevice(6, Computers[6]);
            //Switch01.AddDevice(7, Computers[7]);

            //Switch01.Ports[2].Unplug();

            //foreach (var port in Switch01.Ports)
            //{
            //    Console.WriteLine(port.Status == PortStatus.Occupied
            //        ? $"Port numer {port.Number}, status: {port.Status}, device: {port.Device.Name} {port.Device.MACAddress}"
            //        : $"Port numer {port.Number}, status: {port.Status}");
            //}
            //Console.WriteLine("-----------------");
            //foreach (var computer in Computers)
            //{
            //    Console.WriteLine($"Device {computer.Name} {computer.MACAddress} is free {computer.IsFree()}");
            //}
            //Console.ReadLine();
        }
    }
}
